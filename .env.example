# Environment configuration for PostX Flask App
# Copy this to .env and fill in your values

# Google AI (Gemini) API Key - Get from https://aistudio.google.com/app/apikey
GOOGLE_AI_API_KEY=your_gemini_api_key_here

# Google Cloud Project ID - From your Google Cloud Console
GOOGLE_CLOUD_PROJECT_ID=grand-fx-471616-h8

# Path to Service Account JSON Key File
# Download from Google Cloud Console > IAM & Admin > Service Accounts > Keys
# Ensure the service account has roles: Vertex AI User, Cloud Text-to-Speech API User
GOOGLE_APPLICATION_CREDENTIALS=./grand-fx-471616-h8-69a37d9d94e8.json

# Optional: Enable debug logging (set to 1 for verbose logs)
DEBUG=1

# Optional: Flask secret key for sessions (generate a strong one)
SECRET_KEY=your_secret_key_here_change_this_in_production

# Optional: Rate limiting (e.g., for API endpoints)
RATELIMIT_ENABLED=1